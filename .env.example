# ID da extensão carregada em chrome://extensions (modo desenvolvedor).
# Necessário apenas para testar a página FORA do popup (next dev em http://localhost:3002).
# Copie para .env.local e preencha. Nunca commite o .env.local.
NEXT_PUBLIC_EXTENSION_ID=
